/* Episode Panel - Slide-out Detail View */
/* Fully integrated with the main app's editorial design system */

/* CRITICAL FIX: Remove ALL max-width constraints */
.episode-panel-container * {
    max-width: none !important;
}

/* CSS Variables - Using the main app's color palette */
:root {
    --deep-ink: #1a1a2e;
    --warm-paper: #fafaf9;
    --sage: #4a7c59;
    --dusty-rose: #c77d7d;
    --amber-glow: #f4a261;
    --slate-blue: #5a6c8c;
    --light-sage: #e8f0ea;
    --light-rose: #f5e6e6;
    --gray-600: #6b7280;
    --gray-400: #9ca3af;
    --gray-200: #e5e7eb;
    
    /* Typography scale for better readability at 700px */
    --text-xs: 0.75rem;     /* 12px */
    --text-sm: 0.875rem;    /* 14px */
    --text-base: 1rem;      /* 16px */
    --text-lg: 1.125rem;    /* 18px */
    --text-xl: 1.25rem;     /* 20px */
    --text-2xl: 1.5rem;     /* 24px */
    --text-3xl: 1.875rem;   /* 30px */
}

/* Backdrop */
.episode-panel-backdrop {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.2); /* Lighter backdrop at 20% */
    backdrop-filter: blur(2px); /* Subtle blur */
    -webkit-backdrop-filter: blur(2px);
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1), 
                visibility 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 9998; /* High z-index to ensure it's above all content */
    will-change: opacity; /* Optimize for animation */
}

.episode-panel-backdrop.active {
    opacity: 1;
    visibility: visible;
}

/* Panel Container */
.episode-panel-container {
    position: fixed;
    top: 0;
    right: -70vw; /* Start off-screen to the right */
    width: 70vw; /* Slightly wider for better content fit */
    max-width: 1100px; /* Reasonable max to prevent too wide on large screens */
    min-width: 800px; /* Ensure minimum usable width */
    height: 100vh;
    background: var(--warm-paper);
    box-shadow: -8px 0 32px rgba(26, 26, 46, 0.12);
    transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 9999;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    will-change: right;
}

.episode-panel-container.active {
    right: 0; /* Slide in to visible position */
}

/* Prevent body scroll when panel is open */
body.panel-open {
    overflow: hidden;
}

/* Panel Header - Dark gradient design */
.episode-panel-container .panel-header {
    padding: 24px 30px !important;
    border-bottom: none;
    background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
    color: white;
    flex-shrink: 0;
    width: 100% !important;
    max-width: none !important;
    box-sizing: border-box;
    display: block !important;
}

.episode-panel-container .header-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 18px;
    width: 100% !important;
    gap: 1rem;
}

.episode-panel-container .podcast-info {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex: 0 1 auto; /* Allow shrinking but not growing */
}

.podcast-logo {
    width: 42px;
    height: 42px;
    background: white;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    font-size: 13px;
    font-weight: 700;
    color: #1e293b;
}

.podcast-logo:hover {
    background: linear-gradient(135deg, var(--light-sage) 0%, rgba(74, 124, 89, 0.15) 100%);
    transform: translateY(-1px);
}

.podcast-logo svg {
    width: 28px;
    height: 28px;
    stroke: var(--sage);
}

.podcast-details {
    flex: 1 1 auto;
    min-width: 0;
}

.episode-panel-container .panel-header .podcast-details h3 {
    font-size: 13px;
    font-weight: 600;
    color: white;
    margin: 0 0 3px 0;
    letter-spacing: -0.01em;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    opacity: 0.9;
}

.episode-panel-container .panel-header .podcast-meta {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.4;
}

.episode-panel-container .panel-header .influence-score {
    color: #f4a261; /* Amber for emphasis */
    font-weight: 600;
}

/* Header Actions Container */
.header-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

/* Header Action Buttons */
.header-action-btn {
    width: 36px;
    height: 36px;
    min-width: 36px;
    min-height: 36px;
    padding: 0;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    flex: 0 0 36px;
}

.header-action-btn:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.3);
    transform: scale(1.05);
}

.header-action-btn:focus {
    outline: none;
}

.header-action-btn:focus-visible {
    outline: 2px solid white;
    outline-offset: 2px;
}

.header-action-btn:active {
    transform: scale(0.95);
}

.header-action-btn svg {
    stroke: white;
    transition: stroke 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Close button specific styles */
.episode-panel-container .close-button {
    width: 36px;
    height: 36px;
    min-width: 36px;
    min-height: 36px;
    padding: 0;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    flex: 0 0 36px;
}

.episode-panel-container .close-button:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.3);
    transform: scale(1.05);
}

.episode-panel-container .close-button:focus {
    outline: none;
}

.episode-panel-container .close-button:focus-visible {
    outline: 2px solid white;
    outline-offset: 2px;
}

.episode-panel-container .close-button:active {
    transform: scale(0.95);
}

.episode-panel-container .close-button svg {
    stroke: white;
    transition: stroke 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.close-button:hover svg {
    stroke: var(--sage);
}

.episode-title {
    font-size: 24px;
    font-weight: 600;
    color: white;
    margin: 0 0 12px 0;
    line-height: 1.3;
    letter-spacing: -0.02em;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.episode-guest {
    font-size: 1rem; /* 16px to match reference */
    color: var(--gray-600);
    font-weight: 400;
    margin: 0; /* Reset margins */
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

/* Episode Participants (Host/Guest) */
.episode-participants {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 0;
}

.participant {
    display: flex;
    align-items: baseline;
    gap: 10px;
    font-size: 14px;
    color: rgba(255, 255, 255, 0.9);
}

.participant-label {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.4px;
    opacity: 0.6;
    display: inline-block;
    min-width: 45px;
    color: rgba(255, 255, 255, 0.6);
}

.participant-name {
    font-weight: 400;
    color: white;
    font-size: 14px;
}

/* Panel Content */
/* Panel Content - 2 Column Layout */
.panel-content {
    flex: 1;
    overflow-y: auto;
    padding: 0;
    scroll-behavior: smooth;
    overscroll-behavior: contain;
    display: grid;
    grid-template-columns: 1.6fr 1fr; /* Roughly 62%/38% split */
    gap: 0;
    background: white;
    max-height: calc(100vh - 200px); /* Account for header */
}

/* Main Content Column */
.panel-main-column {
    padding: 30px 35px;
    background: white;
}

/* Sidebar Column */
.panel-sidebar {
    padding: 30px 25px;
    background: #f8fafc;
    border-left: 1px solid #e2e8f0;
}

/* Sidebar section headers */
.panel-sidebar .section-header {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    color: #64748b;
    margin-bottom: 12px;
    padding-bottom: 0;
    border-bottom: none;
}

/* Custom scrollbar for panel content */
.panel-content::-webkit-scrollbar {
    width: 10px;
}

.panel-content::-webkit-scrollbar-track {
    background: rgba(229, 231, 235, 0.3); /* Subtle track */
    border-radius: 5px;
}

.panel-content::-webkit-scrollbar-thumb {
    background: var(--gray-400);
    border-radius: 5px;
    border: 2px solid transparent;
    background-clip: padding-box;
    transition: background 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.panel-content::-webkit-scrollbar-thumb:hover {
    background: var(--gray-600);
}

/* Intelligence Sections */
.intelligence-section {
    margin-bottom: 24px;
}

.intelligence-section:last-child {
    margin-bottom: 0;
}

.section-header {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    color: #64748b;
    margin-bottom: 12px;
    padding-bottom: 0;
    border-bottom: none;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

/* Conversation Section */
.conversation-section {
    background: #f8fafc;
    padding: 18px 20px;
    border-radius: 10px;
    margin-bottom: 24px;
}

.conversation-content {
    font-size: 13px;
    line-height: 1.6;
    color: #334155;
}

/* Insights Section */
.insights-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.insight-item {
    display: flex;
    gap: 14px;
    padding: 14px 16px;
    background: white;
    border: 1px solid #e2e8f0;
    border-radius: 10px;
    margin-bottom: 12px;
    transition: all 0.2s;
}

.insight-item:hover {
    border-color: var(--sage);
    box-shadow: 0 3px 8px rgba(74, 124, 89, 0.08);
}

.insight-number {
    width: 28px;
    height: 28px;
    background: var(--sage);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 13px;
    flex-shrink: 0;
}

.insight-item div:last-child {
    font-size: 13px;
    line-height: 1.5;
    color: #1e293b;
}

/* Quote Section in Sidebar */
.quote-block {
    background: linear-gradient(135deg, #f59e0b 0%, #dc2626 100%);
    padding: 18px 20px;
    border-radius: 10px;
    color: white;
    margin-bottom: 24px;
}

.quote-section .section-header,
.quote-block .section-header {
    color: rgba(255, 255, 255, 0.8);
    border-bottom: none;
    padding-bottom: 0;
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    margin-bottom: 12px;
}

/* Notable Numbers */
.numbers-section {
    margin-bottom: 24px;
}

.numbers-list {
    display: flex;
    flex-direction: column;
}

.number-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 0;
}

.number-label {
    font-size: 13px;
    color: #64748b;
}

.number-value {
    font-size: 16px;
    font-weight: 600;
    color: #1e293b;
}

.number-divider {
    height: 1px;
    background: #e2e8f0;
    margin: 0;
}

/* Compact Mentions Blocks */
.mentions-compact-block {
    background: white;
    padding: 14px 16px;
    border-radius: 10px;
    margin-bottom: 12px;
    cursor: pointer;
    transition: all 0.2s;
}

.mentions-compact-block:hover {
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
    transform: translateY(-1px);
}

.mentions-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.mentions-label {
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.4px;
    color: #64748b;
}

.mentions-count {
    width: 32px;
    height: 32px;
    background: var(--sage);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    flex-shrink: 0;
    font-size: 14px;
}

/* Related Topics */
.topics-section .topics-container {
    background: white;
    padding: 16px 18px;
    border-radius: 10px;
}

.topics-section .section-header {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    color: #64748b;
    margin-bottom: 10px;
    padding-bottom: 0;
    border-bottom: none;
}

.topics-list {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
}

.topic-tag {
    display: inline-block;
    padding: 5px 10px;
    background: #ecfdf5;
    color: var(--sage);
    border-radius: 16px;
    font-size: 12px;
    text-decoration: none;
    transition: all 0.2s;
}

.topic-tag:hover {
    background: var(--sage);
    color: white;
    transform: translateY(-1px);
}

/* Key Points */
.key-points {
    list-style: none;
    padding: 0;
}

.key-points li {
    position: relative;
    padding-left: 2rem; /* Increased indent for better hierarchy */
    margin-bottom: 1.25rem; /* More space between points */
    font-size: var(--text-base); /* 16px for good readability */
    line-height: 1.8; /* Increased line height */
    color: var(--deep-ink);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.key-points li:last-child {
    margin-bottom: 0;
}

.key-points li:before {
    content: "◆";
    position: absolute;
    left: 0;
    color: var(--sage);
    font-weight: bold;
    font-size: 0.875rem;
}

/* Strategic Implications */
.implication-cards {
    display: flex;
    flex-direction: column;
    gap: 1.125rem;
}

.implication-card {
    padding: 1.5rem 1.75rem; /* More generous padding */
    border-radius: 10px;
    font-size: var(--text-base); /* 16px for readability */
    border-left: 4px solid transparent; /* Thicker border for emphasis */
    line-height: 1.7;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.implication-card:hover {
    transform: translateX(4px);
}

.implication-card.investment {
    background: var(--light-sage);
    border-left-color: var(--sage);
    color: var(--deep-ink);
}

.implication-card.investment:hover {
    background: linear-gradient(to right, var(--light-sage) 0%, rgba(74, 124, 89, 0.08) 100%);
}

.implication-card.competitive {
    background: rgba(244, 162, 97, 0.08); /* Amber with better opacity */
    border-left-color: var(--amber-glow);
    color: var(--deep-ink);
}

.implication-card.competitive:hover {
    background: rgba(244, 162, 97, 0.12);
}

.implication-card.benchmark {
    background: rgba(90, 108, 140, 0.05); /* Slate blue with better opacity */
    border-left-color: var(--slate-blue);
    color: var(--deep-ink);
}

.implication-card.benchmark:hover {
    background: rgba(90, 108, 140, 0.08);
}

/* Pattern Recognition */
.pattern-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.125rem;
}

.pattern-card {
    padding: 1.25rem;
    background: white;
    border-radius: 10px;
    border: 1px solid var(--gray-200);
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.pattern-card:hover {
    border-color: var(--sage);
    box-shadow: 0 4px 12px rgba(74, 124, 89, 0.08);
    transform: translateY(-2px);
}

.pattern-number {
    font-size: 2rem; /* Larger for impact */
    font-weight: 700;
    color: var(--sage);
    margin-bottom: 0.5rem;
    line-height: 1;
    font-family: Georgia, 'Times New Roman', serif; /* Editorial number font */
}

.pattern-label {
    font-size: var(--text-sm); /* 14px */
    color: var(--gray-600);
    font-weight: 500;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

/* Portfolio Mentions */
.mentions-empty {
    padding: 2rem;
    background: var(--warm-paper);
    border-radius: 8px;
    text-align: center;
    border: 1px dashed var(--gray-200);
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.mentions-empty:hover {
    border-color: var(--sage);
    background: rgba(232, 240, 234, 0.2);
}

.mentions-empty p {
    color: var(--gray-600);
    margin-bottom: 1rem;
    font-size: var(--text-base); /* 16px for readability */
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.add-portfolio-hint {
    color: var(--sage);
    font-size: var(--text-sm); /* 14px */
    cursor: pointer;
    font-weight: 500;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.add-portfolio-hint:hover {
    text-decoration: underline;
    color: #3d6348;
}

/* Key Takeaways */
.takeaway-list {
    list-style: none;
    padding: 0;
}

.takeaway-list li {
    position: relative;
    padding-left: 1.75rem;
    margin-bottom: 1rem; /* More space between takeaways */
    font-size: var(--text-base); /* 16px for consistency */
    color: var(--deep-ink);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    line-height: 1.7;
}

.takeaway-list li:before {
    content: "•";
    position: absolute;
    left: 0;
    color: var(--sage);
    font-weight: bold;
}

/* Quote Section */
.quote-section {
    background: transparent;
    padding: 0;
    border-radius: 0;
    margin-bottom: 2.5rem;
    border: none;
    position: relative;
    overflow: visible;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.quote-section:hover {
    box-shadow: 0 4px 16px rgba(74, 124, 89, 0.06);
}

.quote-section::before {
    content: '"';
    position: absolute;
    top: -10px;
    left: 10px;
    font-size: 4rem;
    color: var(--sage);
    opacity: 0.1;
    font-weight: 700;
    font-family: Georgia, 'Times New Roman', serif;
}

.quote-text {
    font-size: 15px;
    font-style: italic;
    color: white;
    margin-bottom: 12px;
    line-height: 1.5;
    position: relative;
    z-index: 1;
    font-family: Georgia, 'Times New Roman', serif;
}

.quote-attribution {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.9);
    text-align: right;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.quote-author {
    display: block;
    margin-top: 4px;
    font-size: 13px;
}

.quote-actions {
    display: flex;
    gap: 0.625rem;
}

.quote-action {
    padding: 0.5rem 1rem; /* More generous padding */
    background: white;
    border: 1px solid var(--gray-200);
    border-radius: 6px;
    font-size: var(--text-sm); /* 14px */
    color: var(--gray-600);
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.quote-action:hover {
    border-color: var(--sage);
    color: var(--sage);
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(74, 124, 89, 0.1);
}

.quote-action svg {
    width: 14px;
    height: 14px;
    transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.quote-action:hover svg {
    transform: scale(1.1);
}

/* Related Episodes */
.related-episodes {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.related-episode {
    padding: 1rem 1.25rem; /* More generous padding */
    background: white;
    border: 1px solid var(--gray-200);
    border-radius: 8px;
    font-size: var(--text-sm); /* 14px base */
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.related-episode:hover {
    border-color: var(--sage);
    box-shadow: 0 2px 8px rgba(74, 124, 89, 0.05);
    transform: translateY(-1px);
}

.related-title {
    font-weight: 600;
    color: var(--deep-ink);
    margin-bottom: 0.375rem;
    font-size: var(--text-base); /* 16px for title */
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

.related-note {
    color: var(--gray-600);
    font-size: var(--text-sm); /* 14px for secondary text */
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

/* Market Signals Container */
.market-signals-container {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

/* Audio Progress Bar */
.audio-progress-bar {
    width: 100%;
    height: 2px;
    background: var(--gray-200);
    border-radius: 2px;
    overflow: hidden;
    position: relative;
}

.audio-progress-fill {
    height: 100%;
    width: 0%;
    background: var(--sage);
    transition: width 0.1s linear;
}

/* Audio button specific styles */
.audio-button.playing .play-icon {
    display: none;
}

.audio-button.playing .pause-icon {
    display: block !important;
}

.audio-button.playing {
    color: var(--sage);
    border-color: var(--sage);
}

/* Action Bar - Removed as actions moved to header */
/* Legacy action-bar styles removed */

/* Action buttons removed - functionality moved to header */

/* Responsive Design */
@media (max-width: 1200px) {
    /* Switch to single column layout on tablets */
    .panel-content {
        grid-template-columns: 1fr;
    }
    
    .panel-sidebar {
        border-left: none;
        border-top: 1px solid var(--gray-200);
        padding: 2rem;
    }
}

@media (max-width: 1400px) {
    /* Disable content push on smaller screens */
    body.panel-open main {
        margin-right: 0 !important;
    }
}

@media (max-width: 1024px) {
    .episode-panel-container {
        width: 70vw; /* Larger percentage on medium screens */
        max-width: 600px;
    }
    
    .panel-header {
        padding: 2rem 2rem 1.75rem;
    }
    
    .panel-content {
        padding: 2rem;
    }
    
    .action-bar {
        padding: 1.5rem 2rem;
    }
    
    .episode-title {
        font-size: 1.5rem;
    }
    
    .episode-guest {
        font-size: 1rem;
    }
}

@media (max-width: 768px) {
    .episode-panel-container {
        width: 100%;
        max-width: none;
    }
    
    .panel-header {
        padding: 1.75rem 1.5rem 1.5rem;
    }
    
    .panel-content {
        padding: 1.75rem 1.5rem;
    }
    
    .action-bar {
        padding: 1.25rem 1.5rem;
        gap: 0.75rem;
    }
    
    /* Adjust typography for mobile */
    .episode-title {
        font-size: 1.375rem;
    }
    
    .episode-guest {
        font-size: 0.9375rem;
    }
    
    .section-header {
        font-size: 0.75rem;
        letter-spacing: 1px;
    }
    
    .key-points li,
    .implication-card,
    .takeaway-list li {
        font-size: 0.9375rem;
    }
    
    /* Responsive grids */
    .pattern-grid {
        grid-template-columns: 1fr;
        gap: 0.75rem;
    }
    
    /* Responsive action buttons */
    .action-bar {
        flex-wrap: wrap;
        gap: 0.5rem;
    }
    
    .action-button {
        flex: 1 1 calc(33.333% - 0.334rem); /* 3 buttons per row */
        justify-content: center;
        padding: 0.625rem 0.75rem;
        font-size: 0.75rem;
    }
    
    .action-button.primary {
        flex: 1 1 calc(50% - 0.25rem); /* Primary button takes half width */
    }
    
    .action-button.secondary {
        flex: 1 1 calc(50% - 0.25rem); /* Secondary button takes half width */
    }
    
    /* Adjust spacing */
    .intelligence-section {
        margin-bottom: 2.5rem;
    }
    
    .header-top {
        margin-bottom: 1.5rem;
    }
    
    /* Close button is already the right size */
    
    /* Adjust podcast info */
    .podcast-logo {
        width: 44px;
        height: 44px;
    }
    
    .podcast-logo svg {
        width: 26px;
        height: 26px;
    }
    
    /* Improve mobile readability */
    .quote-text {
        font-size: 1.0625rem;
    }
    
    .pattern-number {
        font-size: 1.5rem;
    }
}

@media (max-width: 480px) {
    /* Extra small screens */
    .panel-header {
        padding: 1.5rem 1.25rem;
    }
    
    .panel-content {
        padding: 1.5rem 1.25rem;
    }
    
    .action-bar {
        padding: 1rem 1.25rem;
    }
    
    .episode-title {
        font-size: 1.25rem;
    }
    
    /* Stack all action buttons on very small screens */
    .action-button {
        flex: 1 1 100%;
    }
}

/* Accessibility */
.episode-panel-container:focus-within {
    outline: none;
}

.episode-panel-container[aria-hidden="true"] {
    pointer-events: none;
}

.episode-panel-container[aria-hidden="false"] {
    pointer-events: auto;
}

/* Focus states for keyboard navigation */
.episode-panel-container *:focus {
    outline: 2px solid var(--sage);
    outline-offset: 2px;
}

/* Animations */
@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0.8;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

@keyframes fadeIn {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
}

@keyframes contentFadeIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Staggered animation for content sections */
.episode-panel-container.active .intelligence-section {
    animation: contentFadeIn 0.4s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    opacity: 0;
}

.episode-panel-container.active .intelligence-section:nth-child(1) {
    animation-delay: 0.1s;
}

.episode-panel-container.active .intelligence-section:nth-child(2) {
    animation-delay: 0.15s;
}

.episode-panel-container.active .intelligence-section:nth-child(3) {
    animation-delay: 0.2s;
}

.episode-panel-container.active .intelligence-section:nth-child(4) {
    animation-delay: 0.25s;
}

.episode-panel-container.active .intelligence-section:nth-child(5) {
    animation-delay: 0.3s;
}

/* Header fade-in animation */
.episode-panel-container.active .panel-header {
    animation: fadeIn 0.3s cubic-bezier(0.4, 0, 0.2, 1) forwards;
}

/* Action bar slide-up animation */
.episode-panel-container.active .action-bar {
    animation: slideUp 0.4s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    animation-delay: 0.2s;
    opacity: 0;
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Loading states */
.panel-content.loading {
    opacity: 0.6;
    pointer-events: none;
}

.panel-content.loading::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 32px;
    height: 32px;
    margin: -16px 0 0 -16px;
    border: 3px solid var(--gray-200);
    border-top-color: var(--sage);
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}

@keyframes spin {
    to {
        transform: rotate(360deg);
    }
}

/* Print styles */
@media print {
    .episode-panel-backdrop,
    .close-button,
    .action-bar {
        display: none !important;
    }
    
    .episode-panel-container {
        position: static;
        width: 100%;
        box-shadow: none;
        transform: none;
    }
    
    .panel-header {
        border-bottom: 2px solid var(--gray-200);
    }
    
    .intelligence-section {
        page-break-inside: avoid;
    }
}